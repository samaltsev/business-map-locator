# Accessibility contract

Every interactive component has keyboard operation, visible focus, semantic labels and non-colour status cues. Overlays trap focus and restore it on close. Map controls have accessible alternatives; motion is optional and respects reduced-motion preferences.
