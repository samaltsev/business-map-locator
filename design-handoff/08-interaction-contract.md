# Interaction contract

Use explicit loading, empty, error, success, disabled and read-only states. Never rely on hover alone. Confirm destructive actions with CMP-ConfirmDialog. Filter changes must be announced and preserve recoverable state.
